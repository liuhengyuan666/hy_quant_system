# Quant System 使用说明

## 1. 项目简介

本项目是一个基于 **pytdx + AKShare + Pandas + PostgreSQL** 的指数 / ETF 量化分析系统，当前支持：

- 历史行情抓取与入库
- 技术指标计算
- 20 个策略统一注册与筛选
- `EOD` 多周期分析（`D / W / M`）
- `INTRADAY` 低频盘中巡检
- `PRE-CLOSE` 收盘前决策分析
- `signal summary` 汇总看板导出
- 本地网页 `dashboard` 实时看板
- `strategy matrix` 全量策略矩阵 Excel 报告
- `daily conclusion` 每日长线 / 短线简明结论
- `data gaps` 数据缺口诊断报告
- 调度运行（盘前 / 盘后 / 盘中循环）

本说明面向日常使用者与维护者，重点说明：**系统能做什么、怎么跑、每个命令输出什么、每份报告怎么看**。

补充说明：

- 大陆数字代码（如 `000300`、`510300`、`159915`）的盘中 `5` 分钟 bars 当前优先使用 `pytdx`
- `AKShare` 仍作为非数字代码与补充兜底来源
- `HSCEI` / `HSTECH` / `HSAHP` 这类港股指数代码，如需稳定实时分钟线，建议后续接入 `Futu OpenAPI / OpenD` 一类券商级数据源

---

## 2. 目录结构

```text
quant-system
├── analysis
├── backtest_engine
├── config
├── core
├── data_service
├── data_storage
├── reports
├── scheduler
├── signal_service
├── strategy_engine
└── tests
```

重点目录：

- `config/`：数据库、运行时、标的池、中文名映射配置
- `data_service/`：历史 / 实时数据抓取
- `data_storage/`：数据库模型、仓储层、实时 bar 存取
- `strategy_engine/`：策略库与注册表
- `signal_service/`：信号生成、汇总、报告导出
- `scheduler/`：盘后任务、盘中循环、调度入口
- `reports/`：所有导出结果目录

---

## 3. 环境准备

### 3.1 安装依赖

```bash
python -m pip install -r requirements.txt
```

### 3.2 启动数据库

```bash
docker compose up -d
```

### 3.3 初始化数据库

```bash
python main.py init-db
```

---

## 4. 关键配置文件

### 4.1 数据库配置

文件：`config/br_db.toml`

用于配置 PostgreSQL 连接。

### 4.2 运行时 / 实时数据源配置

文件：`config/runtime.toml`

当前实时数据源约定：

- 大陆数字代码指数 / ETF：优先走 `pytdx`
- 港股指数类非数字代码：可选走 `Futu OpenAPI / OpenD`

HK 实时可选配置示例：

```toml
[runtime.hk_realtime]
provider = "futu"

[runtime.hk_realtime.futu]
host = "127.0.0.1"
port = 11111
is_encrypt = false

[runtime.hk_realtime.futu.symbol_map]
HSTECH = "<填写 OpenD 中验证通过的代码>"
HSCEI = "<填写 OpenD 中验证通过的代码>"
HSAHP = "<填写 OpenD 中验证通过的代码>"
```

说明：

- 只有在本机已启动 OpenD、并且 symbol 映射填写正确时，港股指数盘中分钟线才会生效
- 如果 `provider = "none"`，系统会继续只使用大陆实时源

### 4.2 标的池配置

文件：`config/universe.toml`

定义当前分析池：

- `index_symbols`
- `etf_symbols`

当前系统所有主要报告（尤其 `daily conclusion`、`preclose`、`strategy matrix`、`data gaps`）都以该配置作为分析范围基准。

### 4.3 标的中文名 / 展示代码配置

文件：`config/symbol_meta.toml`

用于配置：

- `name`
- `display_symbol`

### 4.4 运行时配置

文件：`config/runtime.toml`

关键字段：

- `runtime.timezone`
- `runtime.intraday.enabled`
- `runtime.intraday.interval_minutes`
- `runtime.intraday.bar_frequency`
- `runtime.intraday.lookback_bars`
- `runtime.intraday.window_am_start / end`
- `runtime.intraday.window_pm_start / end`
- `runtime.preclose.enabled`
- `runtime.preclose.trigger_time`
- `runtime.preclose.decision_time`
- `runtime.preclose.output_dir`

---

## 5. 核心概念说明

### 5.1 模式说明

| 模式 | 含义 |
|---|---|
| `EOD` | 盘后 / 日频分析 |
| `INTRADAY` | 盘中低频巡检 |
| `PRE-CLOSE` | 收盘前决策分析 |

### 5.2 周期说明

| 周期 | 含义 |
|---|---|
| `D` | 日频 |
| `W` | 周频 |
| `M` | 月频 |

### 5.3 状态说明

| 值 | 含义 |
|---|---|
| `BUY` | 倾向买入 |
| `SELL` | 倾向卖出 |
| `HOLD` | 倾向观望 |
| `NO_DATA` | 没有历史行情，无法稳定分析 |
| `MISSING` | 理论应有策略信号但缺失，属于异常 |
| `NOT_RUN` | 该模式下该策略没有运行 |
| `N/A` | 该策略不适用于该标的 |

### 5.4 `NO_DATA` 与 `MISSING` 的区别

- `NO_DATA`：上游没有历史行情，策略无法跑
- `MISSING`：理论应有信号，但当前缺失，需要排查生成链路

---

## 6. 当前已接入策略（20 个）

### 6.1 短线策略（short_term）

- `MA_strategy`
- `EMA_cross_strategy`
- `MACD_hist_strategy`
- `RSI_reversion_strategy`
- `Bollinger_breakout_strategy`
- `Bollinger_reversion_strategy`
- `Donchian_breakout_strategy`
- `Momentum_20_strategy`
- `ROC_20_strategy`
- `Volatility_breakout_strategy`
- `KDJ_cross_strategy`
- `CCI_reversion_strategy`

### 6.2 长线策略（long_term）

- `RSRS_strategy`
- `Triple_MA_strategy`
- `Momentum_60_strategy`
- `ATR_channel_strategy`
- `OBV_trend_strategy`
- `ADX_trend_strategy`
- `ETF_rotation_strategy`
- `MultiFactor_strategy`

---

## 7. 全部 CLI 命令说明

以下命令均通过 `python main.py <command>` 执行。

### 7.1 数据与基础能力

#### 1) 初始化数据库

```bash
python main.py init-db
```

功能：创建数据库表结构。

#### 2) 同步历史行情

```bash
python main.py sync-data
python main.py sync-data --start-date 20200101 --end-date 20241231
```

功能：拉取指数 / ETF 历史行情并入库。

#### 3) 计算技术指标

```bash
python main.py calc-indicators
```

功能：为当前标的池刷新技术指标。

#### 4) 查看策略列表

```bash
python main.py list-strategies
```

功能：输出当前注册的全部策略名。

---

### 7.2 信号生成

#### 5) 生成日频信号

```bash
python main.py gen-signals
python main.py gen-signals --strategies MA_strategy,RSRS_strategy
```

功能：生成旧日频主流程信号。

#### 6) 生成多周期 EOD 信号

```bash
python main.py gen-eod-signals
python main.py gen-eod-signals --frequencies D,W,M
python main.py gen-eod-signals --strategies MA_strategy,RSRS_strategy --frequencies D,W
```

功能：生成 `D / W / M` 多周期 EOD 信号。

---

### 7.3 回测

#### 7) 回测（当前示例为 MA）

```bash
python main.py backtest --symbol 510300 --strategy ma
python main.py backtest --symbol 510300 --strategy ma --limit 1200
```

输出包含：

- `metrics`
- `report_path`

---

### 7.4 运行流程

#### 8) 旧版全流程

```bash
python main.py run-pipeline
```

功能：跑数据更新、指标计算、日频信号生成与导出。

#### 9) 旧版全流程 + 分析

```bash
python main.py run-and-analyze
```

功能：在旧流程基础上追加信号分析与二次验证。

#### 10) 盘后 EOD 流程

```bash
python main.py run-eod
python main.py run-eod --frequencies D,W,M
```

功能：盘后跑 `D/W/M` 多周期信号生成与导出。

#### 11) 盘后 EOD + 全量报告导出

```bash
python main.py run-eod-analyze
python main.py run-eod-analyze --frequencies D,W,M
```

当前输出包含：

- `analysis`
- `secondary_validation`
- `summary_path`
- `push_path`
- `preclose_path`
- `preclose_json_path`
- `daily_conclusion_path`
- `daily_conclusion_operation_path`
- `daily_conclusion_json_path`
- `daily_conclusion_xlsx_path`
- `data_gap_path`
- `data_gap_xlsx_path`
- `strategy_matrix_path`

这是**盘后主入口**，适合每日收盘后批量出具完整报告。

---

### 7.5 盘中流程

#### 12) 启动盘中循环

```bash
python main.py run-intraday
python main.py run-intraday --iterations 3
```

功能：按 `runtime.toml` 的盘中节奏持续运行。

#### 13) 执行一次盘中巡检

```bash
python main.py run-intraday-once
```

输出包含：

- `ts`
- `bar_frequency`
- `bar_rows`
- `signal_rows`
- `export_path`
- `summary_path`
- `push_path`

说明：

- 若 `bar_rows > 0`，说明本轮已成功抓到盘中 bar 并完成实时信号计算
- 当前大陆数字代码优先走 `pytdx`，因此通常比基于网页接口的抓取更稳定
- 少数港股指数代码若没有实时 bars，通常表示未启用 OpenD、未配置 `runtime.hk_realtime.futu.symbol_map`，或 OpenD 中对应代码尚未验证通过

若在 preclose 窗口，盘中循环还会自动触发一次收盘前分析。

#### 14) 启动本地 Dashboard

```bash
python main.py run-dashboard
python main.py run-dashboard --host 127.0.0.1 --port 8000
```

启动后打开浏览器：

```text
http://127.0.0.1:8000/
```

页面能力：

- 实时显示最新 `signal_summary / push_candidates / intraday signals`
- 顶部展示 `summary_rows / intraday_signal_rows / active_intraday_count / priority_action_count`
- 支持自动轮询最新报表快照（默认每 `15` 秒一次）
- 支持手动触发一次盘中刷新；按钮会调用当前系统的 `run_intraday_iteration()`，刷新完成后立刻展示新的结果

适用场景：

- 开盘后快速看当前有哪些 `BUY / SELL` 的重点动作
- 查看最新盘中信号是否与 EOD 主方向一致
- 快速确认 `push candidates` 是否发生变化

---

### 7.6 报告导出与 Dashboard

#### 15) 导出信号汇总看板

```bash
python main.py export-signal-summary
```

输出包含：

- `summary_path`
- `push_path`

对应产物：

- `signal_summary`
- `signal_group_summary`
- `signal_top_candidates`
- `signal_push_candidates`

#### 16) 导出收盘前决策报告

```bash
python main.py run-preclose-analysis
python main.py run-preclose-analysis --use-intraday-snapshot
```

输出包含：

- `csv_path`
- `json_path`
- `rows`
- `buy_count / sell_count / hold_count`

#### 17) 导出策略矩阵报告

```bash
python main.py export-strategy-matrix
```

输出包含：

- `xlsx_path`
- `sheet_names`
- `symbol_rows`
- `strategy_count`

#### 18) 导出每日简明结论

```bash
python main.py export-daily-conclusion
```

输出包含：

- `csv_path`
- `operation_csv_path`
- `json_path`
- `xlsx_path`
- `rows`
- `operation_rows`

#### 19) 导出数据缺口诊断报告

```bash
python main.py export-data-gaps
```

输出包含：

- `csv_path`
- `status_csv_path`
- `json_path`
- `xlsx_path`
- `rows`
- `gap_rows`

---

### 7.7 调度

#### 20) 启动调度服务

```bash
python main.py run-scheduler
```

功能：启动定时任务调度。

---

## 8. 各类报告说明

### 8.1 Signal Summary（信号汇总）

目录：`reports/summary/`

主要产物：

- `signal_summary_YYYYMMDD.csv`
- `signal_group_summary_YYYYMMDD.csv`
- `signal_top_candidates_YYYYMMDD.csv`
- `signal_push_candidates_YYYYMMDD.csv`

适用场景：

- 快速看整体市场偏向
- 看二次验证后的主动作
- 看推送候选变化

---

### 8.2 Preclose（收盘前决策）

目录：`reports/preclose/`

主要字段：

- `decision_signal`
- `decision_score`
- `decision_reason`
- `trend_state`
- `latest_price`
- `day_change_pct`
- `distance_to_ma20_pct`
- `eod_d / eod_w / eod_m / intraday`
- `eod_bias`
- `dashboard_action`

说明：

- 盘后模式：按最近闭市交易日做分析
- 盘中模式：允许用当前盘中快照做 preclose 判断
- 若标的缺少历史行情，则当前会输出为 **`HOLD + no_market_data`** 的中性结果，而不是沿用残留旧信号

---

### 8.3 Strategy Matrix（策略矩阵）

目录：`reports/strategy_matrix/`

工作簿当前包含：

- `Overview`
- `Action_Focus`
- `Symbol_Summary`
- `Strategy_Stats`
- `EOD_D_Matrix`
- `EOD_W_Matrix`
- `EOD_M_Matrix`
- `INTRADAY_Matrix`
- `PRE_CLOSE_View`

用途：

- 从“标的 × 策略”角度总览所有策略结果
- 明确看到每个指数 / ETF 在每个策略下的 `BUY / SELL / HOLD`
- 对没有历史行情的标的直接显示 `NO_DATA`

说明：

- 当前矩阵已按**全量标的池**覆盖，而不是只显示有信号的标的
- `INTRADAY_Matrix` 在无盘中时间戳时保持空 sheet

---

### 8.4 Daily Conclusion（每日简明结论）

目录：`reports/daily_conclusion/`

主要产物：

- `daily_conclusion_YYYYMMDD.csv`
- `daily_conclusion_operation_YYYYMMDD.csv`
- `daily_conclusion_YYYYMMDD.json`
- `daily_conclusion_YYYYMMDD.xlsx`

工作簿 sheet：

- `Operation_View`
- `Overview`
- `Daily_Conclusion`
- `LongTerm_Evidence`
- `ShortTerm_Evidence`
- `Data_Gaps`

#### 适合直接看的主视图：`Operation_View`

核心字段：

- `long_term_action`
- `short_term_action`
- `preclose_signal`
- `overall_action`
- `action_note`
- `data_status`

排序逻辑：

- 先显示最强可执行结论
- 再显示单边有方向结论
- 再显示冲突
- 最后显示观望 / `NO_DATA`

---

### 8.5 Data Gaps（数据缺口诊断）

目录：`reports/data_gaps/`

主要产物：

- `data_gaps_YYYYMMDD.csv`：只保留 `NO_DATA / PARTIAL` 问题行
- `data_gap_status_YYYYMMDD.csv`：全量标的状态
- `data_gaps_YYYYMMDD.json`
- `data_gaps_YYYYMMDD.xlsx`

工作簿 sheet：

- `Data_Gaps`
- `Universe_Status`
- `Overview`

关键字段：

- `market_data_rows`
- `data_status`
- `missing_horizons`
- `reason`

用途：

- 识别哪些标的缺少历史行情
- 识别哪些标的是长线 / 短线策略缺失
- 作为上游补数据和排查任务的输入

---

## 9. 每日推荐使用流程

### 9.1 首次搭建

```bash
python main.py init-db
python main.py sync-data
python main.py calc-indicators
python main.py gen-eod-signals --frequencies D,W,M
```

### 9.2 日常盘后完整报告

```bash
python main.py run-eod-analyze --frequencies D,W,M
```

建议查看顺序：

1. `reports/daily_conclusion/...operation...csv`
2. `reports/data_gaps/...csv`
3. `reports/strategy_matrix/...xlsx`
4. `reports/preclose/...csv`
5. `reports/summary/...csv`

### 9.3 盘中巡检

```bash
python main.py run-intraday-once
```

### 9.4 收盘前手动查看一次 preclose

```bash
python main.py run-preclose-analysis --use-intraday-snapshot
```

---

## 10. 结果解读建议

### 10.1 如果只看一个文件

优先看：

```text
reports/daily_conclusion/daily_conclusion_operation_YYYYMMDD.csv
```

这是最适合日常直接看的简化操作视图。

### 10.2 如果要排查异常

优先看：

```text
reports/data_gaps/data_gaps_YYYYMMDD.csv
```

### 10.3 如果要看每个策略细节

优先看：

```text
reports/strategy_matrix/strategy_matrix_YYYYMMDD.xlsx
```

---

## 11. 常见问题

### 11.1 为什么有些标的是 `NO_DATA`？

表示数据库里没有足够的历史行情，系统无法形成稳定结论。

建议：

- 检查 `sync-data` 是否覆盖了该标的
- 用 `export-data-gaps` 定位问题标的

### 11.2 为什么会出现 `MISSING`？

表示理论应有策略信号，但当前缺失，通常是信号生成链路异常或数据 / 周期未闭环。

### 11.3 为什么 Excel 有时导出成新文件名？

如果原文件被 Excel 占用，系统会自动回退到带时间戳的新文件名，避免覆盖失败。

### 11.4 为什么 `INTRADAY_Matrix` 有时是空的？

因为该报告没有匹配到同交易日盘中时间戳，属于正常情况。

---

## 12. 维护与验证建议

每次改动后建议至少执行：

```bash
python -m unittest discover -s tests -p "test_*.py"
python main.py export-daily-conclusion
python main.py export-data-gaps
python main.py export-strategy-matrix
python main.py run-preclose-analysis
```

如果要做盘后全链路验证：

```bash
python main.py run-eod-analyze --frequencies D,W,M
```

---

## 13. 当前最推荐的使用方式

如果你的目标是“每天收盘后快速知道该看什么、怎么操作”，建议固定使用：

1. `python main.py run-eod-analyze --frequencies D,W,M`
2. 打开 `daily_conclusion_operation_YYYYMMDD.csv`
3. 再查看 `data_gaps_YYYYMMDD.csv` 排除数据问题
4. 有需要时再打开 `strategy_matrix_YYYYMMDD.xlsx` 看策略细节

---

如后续继续扩展，建议优先保持：

- `daily conclusion` 负责给出最终可读结论
- `data gaps` 负责解释为什么有些标的不能判断
- `strategy matrix` 负责看底层策略分布
- `preclose` 负责收盘前 10 分钟决策参考
